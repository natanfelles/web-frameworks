language:
  version: 0.5
>>>>>>> 8631ee91 (re-introduce phalcon and spliffy)
  files:
    - "**/*.v"
    - server
  bootstrap:
    - cd /opt/vlang && git fetch --all --tags && git checkout 0.5 && make && v -version && cd /app

  build_flags:

  engines:
    default:
      command: /app/server

framework:
  engines:
    - default
